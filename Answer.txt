answer 1: switch is a newer command and checkout is an older command thats capable of doing more than just switching branches
Answer 2: Git merge combines the changes of the new branch into the main branch
Answer 3: To see local branches, run this command: git branch.
To see remote branches, run this command: git branch -r.
To see all local and remote branches, run this command: git branch -a.
Answer 4: This moves the entire feature branch to begin on the tip of the main branch, effectively incorporating all of the new commits in main . But, instead of using a merge commit, rebasing re-writes the project history by creating brand new commits for each commit in the original branch.
